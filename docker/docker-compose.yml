---
version: "3.8"
services:
  # pgRouting database populated with infrastructure network data
  jore4-mapmatchingdb:
    image: hsldevcom/jore4-postgres:mapmatching-main--5ad5e8cc8d6fffa386c18edf517552a78de65999
    container_name: mapmatchingdb
    restart: "unless-stopped"
    networks:
      - jore4
    ports:
      - "127.0.0.1:19000:5432"
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - DIGIROAD_ROUTING_DUMP_VERSION=2021-10-11

  jore4-mapmatching:
    build:
      context: ".."
      target: production
    container_name: mapmatching
    restart: "unless-stopped"
    networks:
      - jore4
    ports:
      - "127.0.0.1:3200:8080"
    environment:
      - DB_DATABASE=${POSTGRES_DB}
      - DB_HOSTNAME=jore4-mapmatchingdb
      - DB_USERNAME=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}

networks:
  jore4:
